syntax = "proto3";

import "algorithm_state.proto";
// import "matrix.proto";

package spatialcmc;

/* 
 * Whole MCMC chain for a single run of the sampler
 */
message MCMCChain {
  repeated bayesmix.AlgorithmState state = 1;
}